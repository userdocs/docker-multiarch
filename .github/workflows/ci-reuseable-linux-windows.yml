# .github/workflows/dev-linux-windows.yml
name: ci - main reusable caller

on:
  workflow_dispatch:
    inputs:
      source_repo:
        description: "Define the git repo used for the build"
        required: true
        default: "https://github.com/esnet/iperf.git"
      source_branch:
        description: "Specify the branch to build"
        required: true
        default: "master"
      skip_new_tag_check:
        description: "Skip new tag check?"
        required: true
        default: false
        type: boolean
      force_build:
        description: "Force a build"
        required: false
        default: false
        type: boolean
      linux_build_type:
        description: "Build iperf3 using qemu emulation"
        required: true
        default: false
        type: boolean
      artifacts_only:
        description: "No release - artifacts only"
        required: true
        default: false
        type: boolean
      linux:
        description: "Skip Linux build"
        type: boolean
        required: true
        default: false
      windows:
        description: "Skip Windows build"
        type: boolean
        required: true
        default: false
      skip_rerun:
        description: "Skip rerun?"
        required: true
        default: false
        type: boolean
      retries:
        description: "Number of rerun retries"
        required: true
        default: "1"
        type: choice
        options: ["1", "2", "3", "4", "5", "6", "7", "8", "9"]
  schedule:
    - cron: "*/5 * * * *"

permissions:
  actions: write
  id-token: write
  contents: write
  attestations: write
  packages: write

env:
  source_repo: ${{ inputs.source_repo || 'https://github.com/esnet/iperf.git' }}
  source_branch: ${{ inputs.source_branch || 'master' }}
  skip_new_tag_check: ${{ contains(inputs.skip_new_tag_check, 'true') }}
  force_build: ${{ contains(inputs.force_build, 'true') }}
  linux_build_type: ${{ !contains(inputs.linux_build_type, 'false') }}
  artifacts_only: ${{ contains(inputs.artifacts_only, 'true') }}
  linux: ${{ !contains(inputs.linux, 'false') }}
  windows: ${{ !contains(inputs.windows, 'false') }}
  skip_rerun: ${{ !contains(inputs.skip_rerun, 'false') }}
  retries: ${{ inputs.retries || '1' }}

  GH_TOKEN: "${{ github.TOKEN }}"

jobs:
  Check-for-new-iperf3-tag:
    if: env.skip_new_tag_check == 'false'
    uses: ./.github/workflows/ci-check-for-new-release-tag.yml
    with:
      source_repo: ${{ env.source_repo }}
      source_branch: ${{ env.source_branch }}
      force_build: ${{ env.force_build }}

  Linux-Emulation:
    needs: Check-for-new-iperf3-tag
    if: always() && needs.Check-for-new-iperf3-tag.outputs.continue_build == 'yes' &&  env.linux == 'false' && env.linux_build_type == 'true'
    uses: ./.github/workflows/ci-linux-emulation.yml
    secrets:
      vt_api_key: ${{ secrets.VT_API_KEY }}
      GH_TOKEN: ${{ github.token }}
    with:
      source_repo: ${{ env.source_repo }}
      source_branch: ${{ env.source_branch }}
      artifacts_only: ${{ env.artifacts_only }}

  Linux-Crossbuild:
    needs: Check-for-new-iperf3-tag
    if: always() && needs.Check-for-new-iperf3-tag.outputs.continue_build == 'yes' && env.linux == 'false' && env.linux_build_type == 'false'
    uses: ./.github/workflows/ci-linux-crossbuild.yml
    secrets:
      vt_api_key: ${{ secrets.VT_API_KEY }}
      GH_TOKEN: ${{ github.token }}
    with:
      source_repo: ${{ env.source_repo }}
      source_branch: ${{ env.source_branch }}
      artifacts_only: ${{ env.artifacts_only }}

  Docker:
    needs: [Check-for-new-iperf3-tag, Linux-Emulation, Linux-Crossbuild]
    if: always() && (needs.Linux-Emulation.result == 'success' || needs.Linux-Crossbuild.result == 'success') && ( needs.Check-for-new-iperf3-tag.outputs.continue_build == 'yes' && env.linux == 'false' && env.artifacts_only == 'false' )
    uses: ./.github/workflows/ci-docker.yml

  Windows:
    needs: [Check-for-new-iperf3-tag]
    if: needs.Check-for-new-iperf3-tag.outputs.continue_build == 'yes' && env.windows == 'false'
    uses: ./.github/workflows/ci-windows.yml
    secrets:
      vt_api_key: ${{ secrets.VT_API_KEY }}
      GH_TOKEN: ${{ github.token }}
    with:
      source_repo: ${{ env.source_repo }}
      source_branch: ${{ env.source_branch }}
      artifacts_only: ${{ env.artifacts_only }}

  rerun-on-failure:
    needs:
      [
        Check-for-new-iperf3-tag,
        Linux-Emulation,
        Linux-Crossbuild,
        Docker,
        Windows,
      ]
    if: always() && failure() && env.skip_rerun == '0'
    name: rerun-on-failure
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Trigger rerun workflow on job failures
        run: |
          inputs_retries="${{ env.retries }}"
          gh workflow run rerun.yml -f run_id=${{ github.run_id }} -f attempts=${{ github.run_attempt }} -f retries=${inputs_retries:-1}
